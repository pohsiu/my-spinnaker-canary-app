## 1. Pin the upstream commit

- [x] 1.1 Identify the commit to pin against — resolved: `armory/spinnaker-kustomize-patches` commit `50a095cd9aa1a83723f4cc09d6e5a78c6d96d17e` on the `minnaker` branch (current HEAD as of this design; the `armory/minnaker` repo's own tags don't correspond to fixed manifest content, so those aren't what's pinned — see design.md Decisions)
- [x] 1.2 Recorded in `others/minnaker/README.md`'s "What's pinned" table: the commit SHA plus each pin's actual commit/publish date

## 2. k3s-in-Docker service

- [x] 2.1 Created `others/minnaker/docker-compose.yml` with a `k3s` service: `rancher/k3s:v1.31.4-k3s1`, `privileged: true`, named volume for `/var/lib/rancher/k3s`, a `kubeconfig` named volume shared with `bootstrap`/`port-forward`, `--tls-san=k3s` so the cert covers the compose service DNS name, `--disable=traefik` since we use port-forward instead of ingress
- [x] 2.2 k3s API (6443) is reachable via the `k3s` service name on the default compose network — not published to the host (confirmed via `docker compose config`; no `ports:` on the `k3s` service, only `port-forward` publishes to the host)

## 3. Manifests (minnaker's own recipe, pinned + extended)

- [x] 3.1 Add `others/minnaker/manifests/kustomization.yml` — resolved to point at the **repo root** (`github.com/armory/spinnaker-kustomize-patches?ref=...`), not `/recipes` — the repo root's `kustomization.yml` is a symlink to `recipes/kustomization-minnaker.yml`, and kustomize's remote-base fetch requires a default-named file at the target path, which `recipes/` alone doesn't have (confirmed by a failed `kubectl kustomize` run before finding the symlink)
- [x] 3.2 Added `others/minnaker/manifests/patch-canary-storage.yml` (renamed from `patch-canary-prometheus.yml` once its scope grew) — `canary.enabled: true` plus **two** service integrations: `prometheus`/`METRICS_STORE` (upstream's file, `baseUrl` corrected) and `aws`/`CONFIGURATION_STORE`+`OBJECT_STORE` via Minio (**not present in upstream at all** — this turned out to be the actual root cause of the validation failure hit in Verification, not an Operator-version mismatch as first suspected; see README "Current status"). Both integrations combined in one file, not two, because the CRD's `config` field is `x-kubernetes-preserve-unknown-fields: true` (confirmed against the schema directly) — nested arrays get replaced wholesale on patch, not merged, so separate files would clobber each other.
- [x] 3.3 Ran `kubectl kustomize others/minnaker/manifests/` locally (kubectl v1.32's built-in kustomize v5.5.0, no cluster needed) — confirmed: resources render, remote bases resolve, both service integrations present with correct fields
- [x] 3.4 Confirmed in the rendered output, then reconfirmed live post-fix: `canary.enabled: true`, both `prometheus` and `aws` accounts present with correct `supportedTypes`
- [x] 3.5 Added `others/minnaker/manifests/patch-local-access.yml`, overriding `apiSecurity`/`uiSecurity` `overrideBaseUrl` and `corsAccessPattern` to `localhost:8084`/`localhost:9000`. **Fully confirmed working**, not just rendered — see Verification: a real browser session logged into Deck and made a full sequence of authenticated Gate API calls with zero errors.
- [x] 3.6 Added `others/minnaker/manifests/patch-mysql-arm64.yml` (mysql:5.7 → 8.0.36 + `mysql_native_password`), five split `patch-delete-*.yml` files (incompatible `Ingress` + 5 namespace-conflicting Grafana resources — a combined multi-doc file crashed kustomize with a segfault, confirmed and fixed by splitting), `patch-spinnaker-version.yml` (2.21.4 → 2.34.0, BOM no longer resolvable), and `patch-disable-terraform.yml` (armory/terraformer has no ARM64 image and blocked the aggregate health check) — all discovered and fixed during actual `docker compose up` runs, none anticipated during design

## 4. Bootstrap container

- [x] 4.1 Wrote `others/minnaker/bootstrap/entrypoint.sh`: `wait_for()` helper polls `kubectl get nodes` (60 attempts, 5s apart = 5 min bound, clear error on timeout, not an infinite silent loop). Passes `shellcheck` clean.
- [x] 4.2 **Corrected mid-implementation**: the OSS operator (`armory/spinnaker-operator`) registers `spinnaker.io` CRDs, but our manifests use `apiVersion: spinnaker.armory.io/v1alpha2` (traced through `spinnaker-kustomize-patches`' `deploy.sh` flavor switch) — that needs the **Armory-flavor** operator, `armory-io/spinnaker-operator` (confirmed public, not a private/licensed repo). Pinned to its latest release `v1.8.11` (published 2025-12-04), confirmed by downloading `manifests.tgz` and grepping its CRD's `group: spinnaker.armory.io` directly, not assumed. Entrypoint fetches the patches repo's `operator/{kustomization.yml,patch-config.yaml,halyard-local.yml}`, lays the downloaded release's `deploy/` alongside them (matching `deploy.sh`'s own directory layout), and applies with `kubectl apply -k`. Also creates the `spinnaker` namespace and a `spin-secrets` Secret from upstream's `secrets-example.env` (disposable placeholder values, not real credentials — required because the recipe's `SpinnakerService` references several `encrypted:k8s!n:spin-secrets!k:...` fields that fail to resolve without it), then `kubectl apply -k /manifests`.
- [x] 4.3 Added a readiness loop polling `spinsvc spinnaker`'s `.status.status` field for `"OK"` (confirmed as a real field via the CRD's OpenAPI schema, not guessed) — **confirmed live**: watched it transition `<none yet>` → `Updating` → `Failure` (during a rollout, and once for real due to the terraformer issue) → `Updating` → `OK`, exactly matching the intended states, not just the one value guessed at design time.
- [x] 4.4 Added `others/minnaker/bootstrap/Dockerfile` (Alpine + curl/tar/bash + a pinned `kubectl` binary) and wired both `bootstrap` and `port-forward` services in `docker-compose.yml` to build from it. Validated with `docker compose config` (syntax + variable interpolation, e.g. `$$` escaping in the port-forward service's inline script) — not yet run against a live daemon (see Verification).

## 5. Host access

- [x] 5.1 Added the `port-forward` service: waits on `bootstrap` via `condition: service_completed_successfully`, runs both `kubectl port-forward` commands in a retry loop (`wait -n` + restart on either exiting, rather than a one-shot forward that silently dies if a pod restarts)
- [x] 5.2 Published `8084:8084` and `9000:9000` on the `port-forward` service in `docker-compose.yml`

## 6. Documentation

- [x] 6.1 Finished `others/minnaker/README.md`: prerequisites stated plainly (including this dev machine's actual measured ~11GB free disk vs. the 30GB needed — not glossed over), a pin table with the *why* for each pinned version, quickstart, teardown
- [x] 6.2 Documented the scope boundary in a "What this does NOT do" section: Spinnaker/Kayenta control plane only, no `apps/backend` deployment or Prometheus wiring, no Dinghy, plus every specific thing that was verified by static analysis vs. actually confirmed live
- [x] 6.3 Cross-linked: root `README.md`'s Helm+Spinnaker section now points at `others/minnaker/README.md`; `CLAUDE.md` lists `others/minnaker/` in the layout table and adds a note under "Environment limits" about the disk constraint and what was/wasn't actually run

## 7. Verification

- [x] 7.1 Ran `docker compose up -d` for real against a live Docker daemon (OrbStack, 10 CPU / 15.66GB). `bootstrap` exits **0** — `SpinnakerService is healthy`. Took several iterations fixing real bugs along the way (see `others/minnaker/README.md`'s "Current status" table) before reaching this.
- [x] 7.2 `curl http://localhost:9000/` → `200` (Deck). `curl http://localhost:8084/api/health` → `200`, `{"status":"UP"}` (Gate). Went further than the task asked: logged into Deck through an actual Playwright browser session and confirmed a full sequence of authenticated Gate API calls succeed with zero errors.
- [x] 7.3 Confirmed live, not just rendered: Kayenta's pod is `1/1 Running`; its own `/health` endpoint (hit via `kubectl exec`) reports `"status":"UP"` with `configServer: UP` — meaning it's actually reading from the Minio config/object store, not just that the process started. Gate's `/api/credentials` (authenticated) returns the registered accounts.
- [ ] 7.4 **Not run** — the live cluster was deliberately left running after reaching a healthy state, for further exploration rather than torn down immediately. Run `docker compose down -v` in `others/minnaker/` when done with it; nothing else in this verification depends on tearing down first.
- [x] 7.5 **Run for real this session and reached a fully healthy end state.** Fixed 9 concrete bugs only discoverable by actually running it end-to-end (missing `git` binary, a kustomize segfault on multi-doc delete patches, ARM64-incompatible `mysql:5.7` and `armory/terraformer`, a removed `Ingress` API version, a namespace-conflicting Grafana resource set, k8s 1.24+'s ServiceAccount token behavior change, an unresolvable hardcoded Spinnaker version, and — the actual root cause of the original blocker — a missing Kayenta storage account that upstream's own recipe never provided an example for) — each confirmed via direct cluster inspection or a real browser session, not guessed. Also ruled out one full alternative approach (pinning an Operator version contemporary with the recipe) as a genuine dead end, confirmed by swapping the live Operator image and watching it crash, not assumed. See `others/minnaker/README.md` for the complete, current, honest state — including exactly what's still out of scope (`apps/backend` deployment, real Prometheus scrape targets, Dinghy).
